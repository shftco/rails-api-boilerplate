# frozen_string_literal: true

require "dry/monads/do"

class <%= service_class_name %>Service::List
  include Dry::Monads[:result]
  include Dry::Monads::Do.for(:call)

  def call(pagination: nil, filter: nil, sort: nil)
    # Success(records.page(pagination.page).per(pagination.per_page))
  end
end
