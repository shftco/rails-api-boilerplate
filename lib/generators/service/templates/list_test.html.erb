# frozen_string_literal: true

require "test_helper"

class <%= service_class_name %>::ListTest < ActiveSupport::TestCase
  def setup
  end

  test 'should list all records' do
  end

  test 'should list records with using pagination parameters' do
  end

  test 'should list records with using sorting parameters' do
  end

  test 'should list records with using filtering parameters' do
  end
end
